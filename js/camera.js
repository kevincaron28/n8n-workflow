// Camera tile — Phase 5 (docs/project-brief.md §5.5). Blocked on brief §11.2 (camera brand/model).
// LAN-only; never commit real feed URLs/credentials outside config.js. Not wired into app.js yet.

export function initCamera(_config, _root) {
  // TODO(phase 5): HTTP snapshot polling into <img src> with a cache-busting query param.
}
