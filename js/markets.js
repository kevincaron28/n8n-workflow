// Markets strip — Phase 4 (docs/project-brief.md §5.4). Finnhub quotes for the ETF proxies in
// config.markets.symbols, polled only during market hours. Not wired into app.js yet.

export function initMarkets(_config, _root) {
  // TODO(phase 4): poll config.markets.symbols 09:30-16:00 ET weekdays only; show last close otherwise.
}
