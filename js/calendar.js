// Google Calendar tile — Phase 3 (docs/project-brief.md §5.3). Blocked on brief §11.4
// (whether a dedicated public "Maison" calendar is acceptable). Not wired into app.js yet.

export function initCalendar(_config, _root) {
  // TODO(phase 3): GET /calendar/v3/calendars/{id}/events with a referrer-restricted API key.
}
